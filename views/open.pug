doctype html
html
    head
        title XLink Menu
        link(rel='stylesheet', href='/static/style.css')
        link(rel='stylesheet', href='/static/fontawesome-pro-6.5.1-web/css/all.css')
        script(rel='script', href='/static/menu.js')
        script(rel='script', href='/static/fontawesome-pro-6.5.1-web/js/all.js')
    body
        div.header-bar
            if (key)
                span AmusementVISION
            else
                span Global Management
            div(style="float: right;")
               span XLink
        div.error-box
            div
                i.fa-regular.fa-loader.fa-2x.fa-spin
            div.error-text
                span Connecting...
        if (key)
            script.
                window.location.pathname = `/device/!{key}`
        else
            script.
                window.location.pathname = `/`
